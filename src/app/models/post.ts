export type Posts = {
    id: number
    title: string
    body: string
    userId: number
    date: number
    category: string
   
  }

  export type PostCategories ={
    id: string
    title: string
  }
